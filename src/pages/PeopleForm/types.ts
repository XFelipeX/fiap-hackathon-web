export interface TeacherFormValues {
  name: string
  email: string
  birthDay: string
  tel: string
  subjects: []
}

export interface StudentFormValues {
  name: string
  email: string
  birthDay: string
  tel: string
}